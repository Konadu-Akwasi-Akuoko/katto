{
  "name": "@hyper-frames/thumbnail-render",
  "version": "0.14.0",
  "description": "Render hyper-frames thumbnail HTML to 1280x720 PNG via Playwright. Brand-agnostic; the channel's visual signature lives in the thumbnail-and-title-generator skill, not here.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync-icons": "node scripts/sync-icons.mjs"
  },
  "dependencies": {
    "playwright": "1.49.1",
    "lucide": "0.462.0"
  }
}
